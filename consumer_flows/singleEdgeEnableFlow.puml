@startuml
box "VCO"
participant "kafka/redis streams" as kafka
participant "Arista FW consumer" as consumer
participant "IAM" as IAM
participant "mysql" as mysql
end box
box "Arista FW"
participant "Arista FW Backend" as arista_fw_backend
end box
activate consumer #LightBlue
consumer <- kafka : Consume new message
consumer -> consumer : Validate if enterprise has arista fw license
consumer <-> IAM : Generate Signed JWT
consumer -> arista_fw_backend: Send request to create appliance with auth header
arista_fw_backend -> arista_fw_backend: Create appliance
arista_fw_backend -> consumer: Response \n (with appliance id)
consumer -> mysql: store applianceID in EDGE_PROPERTIES table \n if does not exist
deactivate consumer

@enduml 

